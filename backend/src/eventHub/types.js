const CAP_SEVERITY = ['Minor', 'Moderate', 'Severe', 'Extreme'];
const CAP_URGENCY = ['Immediate', 'Expected', 'Future', 'Past'];
const CAP_CERTAINTY = ['Observed', 'Likely', 'Possible', 'Unlikely'];

const SOURCE_PRIVACY_LEVELS = ['public', 'aggregated', 'trusted-only'];

module.exports = {
  CAP_SEVERITY,
  CAP_URGENCY,
  CAP_CERTAINTY,
  SOURCE_PRIVACY_LEVELS,
};
